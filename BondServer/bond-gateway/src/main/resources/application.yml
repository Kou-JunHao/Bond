server:
  port: 8080

spring:
  application:
    name: bond-gateway
  cloud:
    gateway:
      routes:
        - id: bond-auth
          uri: lb://bond-auth
          predicates:
            - Path=/api/auth/**,/api/devices/**,/api/friends/**
        - id: bond-transfer
          uri: lb://bond-transfer
          predicates:
            - Path=/api/transfer/**,/api/workspaces/**,/api/admin/**,/api/invite/**
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_UNIQUE
